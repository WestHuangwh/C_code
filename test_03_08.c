#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>
int memcpy(char* arr1, int num)
{
	while (*arr1 < 100)
	{
		int left = num - 1;
		int right = num;
		arr1++;
		int max = *arr1;
		for (int i = 0; i < num; i++)
		{
			printf("%d\n", *arr[i]);

		}
	}
	return strlen;
}
int my_memcpy(char* arr2, int str)
{
	while (*arr1 < 100)
	{
		int left = num - 1;
		int right = num;
		arr1++;
		int max = *arr1;
		for (int i = 0; i < num; i++)
		{
			printf("%d\n", *arr[i]);

		}
	}
	return strlen;
}
int main(void)
{
	char arr[20] = { 0 };
	char arr1[10] = "abcdefg";

	memcpy(arr, sz);
	int ret = memcpy(arr, sz);
	printf("%d", ret);

	if (ret == 5)
	{
		memcpy(ret);


	}
	return 0;
}